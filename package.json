{
  "name": "share-house",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=dev nodemon --delay 3000 ./bin/www",
    "start": "cross-env NODE_ENV=production pm2 start ./bin/www --watch ",
    "pm2": "pm2 list",
    "stop": "pm2 stop all"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "pm2": "^5.3.0",
    "winston": "^3.9.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22"
  }
}
